/**
 * Bridge file for migration.
 * Maps the old import path to the new layer2/ directory structure.
 * This file allows page.tsx to remain unmodified (as per user constraint).
 */
export { Layer2Container } from './layer2';
